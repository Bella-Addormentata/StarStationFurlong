/**
 * Network Provider — historical stub (this demo never implemented networking).
 * The live typed port lives in 0.5.0-core-loop-demo/src/network/ (v006 §12.2):
 * raw WebTransport + serverCertificateHashes, not simple-peer.
 */

export class NetworkProvider {
  constructor() {
    console.log('⚠️  NetworkProvider - Not implemented (see 0.5.0-core-loop-demo for the live seams)');
  }
}
